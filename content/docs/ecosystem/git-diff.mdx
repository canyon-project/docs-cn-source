# @canyonjs/git-diff

@canyonjs/git-diff 是一个用于在 CI/CD 流水线中检测代码变更并生成 Git Diff 内容的 NPM 包


## 使用

### 在 CI/CD 流水线中使用

```bash
npx @canyonjs/git-diff
```

## 原理

检测当前流水线变量，确定提供商，根据提供商的CI环境变量，当前是MR还是commit，来判断需要git diff所对比的after...now的commit参数。

目前支持github action、gitlab runner