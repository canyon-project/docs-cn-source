import { Steps, Callout, Cards } from "nextra/components";

# 首次获取覆盖率数据

> [!NOTE]
>
> Canyon 提供了[常见框架的安装指南](/docs/installation/getting-started)，帮助您快速上手。

按照以下步骤完成您的首次覆盖率数据报告：

## 新项目开始

<Steps>
  ### 安装

Babel 是前端工程化和模块化开发中不可或缺的工具。对于 Babel 项目，您只需安装两个 Babel 插件即可快速开始。

```sh npm2yarn
npm install babel-plugin-istanbul babel-plugin-canyon -D
```

将 [`istanbul`](https://www.npmjs.com/package/babel-plugin-istanbul) 和 [`canyon`](https://www.npmjs.com/package/babel-plugin-canyon) 插件添加到您的 [Babel 配置文件](https://babeljs.io/docs/config-files#configuration-file-types)中：

  ```js filename="babel.config.js" {3,4} copy
  module.exports = {
    plugins: [
      'istanbul',
      'canyon'
      // 注意插件顺序：canyon 插件应在 istanbul 插件之后
      ],
  };
  ```

### 验证

配置完成后，启动您的项目并在控制台中打印 **window.\_\_coverage\_\_**。如果输出与下图匹配，则代码插桩成功。

![coverage-canyon-console](https://cdn.jsdelivr.net/gh/canyon-project/assets/docs/static/docs/getting-started/first-coverage/coverage-canyon-console.png)

### 配置 CI 环境变量

在 CI 环境中，我们需要配置一些环境变量来报告覆盖率数据。

> [!TIP]
>
> Canyon 在编译时检测流水线变量，我们支持多种[流水线提供商](/docs/reference/provider)。<br/>
> 没有看到您的流水线提供商？试试[显式配置](/docs/ecosystem/babel-plugin-canyon#配置)。


1. `DSN` 和 `REPORTER`

- **DSN**：覆盖率数据报告的服务地址，格式为 \{\{url\}\}/coverage/client，其中 \{\{url\}\}\ 是 Canyon 服务地址。
- **REPORTER**：用户令牌，可以在 Canyon 用户设置页面查看。

2. 配置 CI 平台变量

![gitlab](https://cdn.jsdelivr.net/gh/canyon-project/assets/docs/static/docs/getting-started/first-coverage/gitlab-var-config.png)

<Callout type="info" emoji="ℹ️">
  项目 ID、分支和 SHA 无需手动配置；Canyon 插件会自动检测流水线环境变量。
</Callout>

  ### 更新 Babel 插件激活条件

  __在 CI 阶段，我们需要控制插件激活条件，以防止在生产分支中进行插桩。__

  ```js {2,3} copy
  module.exports = {
    plugins: (process.env.CI &&
              process.env.CI_COMMIT_REF_NAME !== 'release')
              ? [
                'istanbul',
                'canyon'
              ]:[],
  };
  ```

  ### 报告覆盖率数据

  等待 CI 完成后，将页面部署到测试环境。

  此时，覆盖率数据存储在浏览器中。随着用户交互或 UI 自动化测试的执行，window.__coverage___ 数据将不断累积。

  将这些数据准确报告到 Canyon 服务器，即可实时显示覆盖率数据。


  以下是几种报告覆盖率数据的方式：

  <Cards>
  <Cards.Card
    title="UI 自动化测试"
    href="/docs/end-to-end-testing/playwright"
  />
  <Cards.Card
    title="手动测试"
    href="/docs/ecosystem/canyon-extension"
  />
</Cards>
</Steps>
